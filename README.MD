Packages:  
    `npm install typescript --save-dev`  
    `npm install terser -g`  
    `npm install vite --save-dev`  
  
^ Use npm install for quick install.  
  
Scripts:  
    `npm run compile`  
    `npm run build`  
    `npm run dev`  
  
^ Ways to compile.  
  
Use `serve .` to run on localhost.  