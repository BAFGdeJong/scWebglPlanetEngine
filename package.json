{
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "^5.8.3",
    "vite": "^5.4.19"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "build": "vite build --config vite.config.ts && terser dist/planetEngine.iife.js -o dist/planetEngine.min.js --compress --mangle",
    "dev": "npm run check && vite build --config vite.config.dev.ts && vite --config vite.config.dev.ts"
  },
  "dependencies": {
    "terser": "^5.44.0",
    "wgpu-matrix": "^3.4.0"
  }
}
